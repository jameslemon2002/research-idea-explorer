interface:
  display_name: "Research Idea Explorer"
  short_description: "Search literature, build one strong frontier, then deepen only when needed"
  default_prompt: "Use $research-idea-explorer to search literature, build one strong frontier from several orthogonal research moves, and only branch into a deeper second pass if a direction deserves more exploration."

policy:
  allow_implicit_invocation: true
